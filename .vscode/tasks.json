{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceRoot}/build.sh",
            //"args": [ "UnixSocketTransportTest.out" ],
            "args": [ "TcpIpTest.out" ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}