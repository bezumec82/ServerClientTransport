{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceRoot}/build.sh",
            "args": [ "UnixSocketTransportTest.out" ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}